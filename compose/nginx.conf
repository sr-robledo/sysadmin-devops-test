# Reverse proxy mínimo delante de la API.
# Puedes ampliarlo (cabeceras, límites, logs, TLS...) si lo consideras necesario.

server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://api:8080;
        proxy_set_header Host $host;
    }
}
